--

- name: Phase I of I - Installing zsh.
  hosts: linux
  become: true
  tasks: 
  - name: Installing zsh and dependencies (git-core, curl, fonts-powerline).
    apt:
      name:
        -git-core 
        - curl 
        - fonts-powerline
        - zsh
      state: present
      update_cache: yes # Simulate apt-update on the first run.
